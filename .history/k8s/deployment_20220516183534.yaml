apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctse-order
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ctse-order
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5    
  template:
    metadata:
      labels:
        app: ctse-order
    spec:
      containers:
      - name: github-actions-order 
        image: lakindu23/ctse-order:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m